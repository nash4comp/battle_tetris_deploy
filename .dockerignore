# 패키지 의존성을 이미 빌드할 것이므로 _build 디렉토리는 무시합니다.
_build/

# 컴파일된 애플리케이션 바이너리 파일은 이미 빌드했으므로 디렉토리를 무시합니다.
deps/

# 테스트 결과, 로그 파일 등을 무시합니다.
log/
test/

# 개발 환경 설정 파일은 이미 복사되었으므로 무시합니다.
config/dev.secret.exs

# 개발 서버에서 사용하는 데이터베이스 파일을 무시합니다.
priv/repo/migrations/

# 개발 서버에서 사용하는 데이터베이스 파일을 무시합니다.
_dev/
